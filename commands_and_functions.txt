!hello = (mocked out, just needs the string updated with instructions. might not even keep this one.)
!help = (no function yet)
!new_character = (no function yet but is mocked out)
!new_npc = (no function yet)
!get_characters = get_characters
!get_npcs = (no function yet)
!statblock {first_name} = (mocked out but needs to be built in database_functions)
!roll = partially built under "Rolling function" comment. Doesn't allow for modifiers yet.